
module.exports = function MarsRover (gridDimension = {with: 10, heightt: 10},
	initialPosition = {x: 0, y: 0},
	initialFacing = 'N') {

	// initialization module code ... execute only once

	return {

		exec (commands) {

			const cmds = commands.split('')
			return {}
		}
	}
}
